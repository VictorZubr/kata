const data = [{age: 18, rate:30}, {age: 18, rate:15}, {age: 50, rate:35}]

const groupByAge = (data) => {
    // return data.reduce((acc, {age, rate}) => ((acc[age] = acc[age] || []).push(rate), acc), {})
    return 1,2, 3;
}

console.log(groupByAge(data))